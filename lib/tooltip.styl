transform($val) {
  -webkit-transform: $val;
  -moz-transform: $val;
  -ms-transform: $val;
  -o-transform: $val;
  transform: $val;
}

tooltip($selector, $mouseOver = false) {
  error('bad type for $selector') unless $selector is a 'string'

  position: relative;
  display: inline-block;
  & {$selector} {
    position: absolute;
    bottom: 100%;
    left: 50%;
    visibility: hidden;
    white-space: nowrap;
    transform: translateX(-50%);
    unless ($mouseOver) {
      pointer-events: none;
    }
  }
  &:hover, &:focus {
    {$selector} {
      visibility: visible;
    }
  }
}
